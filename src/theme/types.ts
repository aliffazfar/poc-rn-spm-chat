export type ThemeMode = 'light' | 'dark' | 'system'

export interface ThemeColors {
  background: string
  surface: string
  surfaceVariant: string
  card: string
  text: string
  textMuted: string
  textSubtle: string
  border: string
  input: string
  primary: string
  accent: string
  danger: string
  chevron: string
}
